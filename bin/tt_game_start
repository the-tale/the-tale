#!/bin/bash

set -e

trap 'echo "\"${last_command}\" command failed with exit code $?."' ERR

# up core services
./docker_compose.sh up -d

# up services
./docker_compose.sh --profile services up -d
./docker_compose.sh up -d site
./docker_compose.sh --profile workers up -d
