{
    "database": {"name": "{{user}}",
                 "user": "{{user}}",
                 "password": "{{user}}",
                 "host": "localhost",
                 "port": 5432,
                 "minsize": 1,
                 "maxsize": 2,
                 "timeout": 10 },

    "log": {"level": "{{tt_log_level}}"},

    "service": {"port": {{port}}},

    "custom": {{tt_services[user].get('custom')|to_nice_json}}
}
