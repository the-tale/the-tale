{
    "database": {"name": "{{user}}",
                 "user": "{{user}}",
                 "password": "{{user}}",
                 "host": "localhost",
                 "port": 5432,
                 "minsize": 2,
                 "maxsize": 5,
                 "timeout": 60 },

    "log": {"level": "{{tt_log_level}}"},

    "service": {"port": {{port}}},

    "custom": {{tt_services[user].get('custom')|to_nice_json}}
}
